import { AddButton } from '../AddButton';

export const AddTechSkills = () => {
    return (
        <AddButton ariaLabel={'add-tech-skills-button'}>
            Add Tech Skills
        </AddButton>
    );
};
