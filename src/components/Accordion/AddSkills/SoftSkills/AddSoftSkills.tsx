import { AddButton } from '../AddButton';

export const AddSoftSkills = () => {
    return (
        <AddButton ariaLabel={'add-soft-skills-button'}>
            Add Soft Skills
        </AddButton>
    );
};
